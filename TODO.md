# TODO

- [ ] Update OAuth consent screen app name from "Trouve-Tout" to "MyTubes" in Google Cloud Console (APIs & Services > OAuth consent screen)
- [ ] Note: Adding `drive.appdata` scope requires existing users to re-authorize on next sign-in
